services:
  postgres:
    image: postgres:latest
    restart: always
    environment:
      POSTGRES_DB: "avito"
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "admin"
    ports:
      - "5432:5432"
    volumes:
      - ./initdb:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d avito"]
      interval: 5s
      timeout: 5s
      retries: 5

  app:
    build: ./
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      POSTGRES_CONN: "postgres://postgres:admin@postgres:5432/avito?sslmode=disable"
    ports:
      - "8080:8080"
